---
description: Example command — replace this with your own
argument-hint: "[optional argument]"
allowed-tools: Bash, Read
---

Your command instructions go here. Claude will follow these when you run `/example`.

You can reference arguments with `$ARGUMENTS`.